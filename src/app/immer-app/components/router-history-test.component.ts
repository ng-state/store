import { Component } from '@angular/core';

@Component({
  selector: 'router-history-test',
  template: `<div>This is router history test for window.state.showHistory()</div>`
})

export class RouterHistoryTestComponent {

}